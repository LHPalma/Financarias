{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // ATENÇÃO: pepper EXPOSTO DE PROPÓSITO, para fins de aprendizado.
  // Um pepper só protege enquanto é segredo: este, versionado no repositório, não protege nada.
  // Existe para a aplicação subir em Development sem configuração extra e para estudar a técnica.
  // Em produção: NÃO reutilizar este valor. Gerar um pepper novo, configurá-lo fora do repositório
  // (variável de ambiente PasswordHashing__Peppers__N) e rotacionar — a versão exposta deve ser
  // tratada como comprometida. Este arquivo só é carregado em Development, então em Production o
  // boot continua falhando até existir um pepper real configurado.
  "PasswordHashing": {
    "CurrentPepperVersion": 1,
    "Peppers": {
      "1": "XQxsnW6NltQ8wt6pNyXVt+hbsZlOQJFk4gTatnziNFI="
    }
  },
  // ATENÇÃO: chave de assinatura do JWT EXPOSTA DE PROPÓSITO, para fins de aprendizado — mesmo caso do pepper.
  // Com esta chave qualquer pessoa forja um token válido como qualquer usuário, então ela não protege nada.
  // Existe para a aplicação subir em Development sem configuração extra. Em produção: NÃO reutilizar este
  // valor; gerar uma chave nova (base64 de 32+ bytes aleatórios), configurá-la fora do repositório
  // (variável de ambiente Jwt__SigningKey) e tratar esta como comprometida. Este arquivo só é carregado em
  // Development, então em Production o boot continua falhando até existir uma chave real configurada.
  "Jwt": {
    "SigningKey": "C9/5PBvbrpYpGgaaoeV8MvMlxi5NZy0+AH5g6JO4lYo="
  }
}
